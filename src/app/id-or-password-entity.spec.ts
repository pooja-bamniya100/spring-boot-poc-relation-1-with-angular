import { IdOrPasswordEntity } from './id-or-password-entity';

describe('IdOrPasswordEntity', () => {
  it('should create an instance', () => {
    expect(new IdOrPasswordEntity()).toBeTruthy();
  });
});
